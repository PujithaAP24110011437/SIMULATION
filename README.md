# SIMULATION
This project simulates the working of a linked list data structure. It demonstrates how nodes are created, inserted, searched, updated, and deleted dynamically using pointers. The simulation helps visualize memory allocation and pointer manipulation, providing a clear understanding of how linked lists operate internally.
